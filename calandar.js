class Calendar{
    constructor(dia_actual,num_dias){
        this.dia_actual = dia_actual
        this.num_dias = num_dias
    }

    const = que_dia => (dia_actual,num_dias) {
        let conteo = num_dias%dia_actual
        return dia_actual + conteo
    }
}